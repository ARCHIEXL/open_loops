<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Open Loops</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Fonts (Suisse Intl not shipped; Inter + IBM Plex Mono per style guide) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#1A1A1A" media="(prefers-color-scheme: dark)">
    <meta name="theme-color" content="#F5F4F2" media="(prefers-color-scheme: light)">
    <link rel="apple-touch-icon" href="/apple/apple-touch-icon.png">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
  </head>

  <body class="app">
    <header class="app-header">
      <div class="container header-inner">
        <!-- Left: logomark -->
        <a class="brand brand--mark" href="<%= root_path %>" aria-label="Open Loops (Now)">
          <%= image_tag "brand/logomark-dark.png",
                        class: "brand-logo brand-logo--dark",
                        alt: "" %>

          <%= image_tag "brand/logomark-light.png",
                        class: "brand-logo brand-logo--light",
                        alt: "" %>
        </a>

        <!-- Right: nav -->
        <nav class="top-actions" aria-label="Primary">
          <%= link_to inbox_path, class: "icon-btn", aria: { label: "Inbox (Unscored)" } do %>
            <svg class="icon" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M4 4h16v10h-4l-2 3h-4l-2-3H4V4z" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
            </svg>
            <span class="sr-only">Inbox</span>
          <% end %>

          <%= link_to archive_path, class: "icon-btn", aria: { label: "Archive (Later and Completed)" } do %>
            <svg class="icon" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M4 7h16v14H4V7zm2-4h12l2 4H4l2-4z" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
            </svg>
            <span class="sr-only">Archive</span>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>
